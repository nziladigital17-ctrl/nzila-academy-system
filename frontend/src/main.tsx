import React from 'react';
import ReactDOM from 'react-dom/client';

/**
 * Nzila Academy — Frontend Entry Point
 *
 * This is a minimal entry point. Pages, components, routes, and styles
 * will be added in the next development phase.
 */
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>Nzila Academy</h1>
      <p>Frontend em construção.</p>
    </div>
  </React.StrictMode>,
);
