# URL base da API Laravel.
# Em desenvolvimento, o proxy do Vite redireciona /api/* automaticamente.
# Em produção, deve apontar para o domínio real da API (ex: https://api.dominio.ao).
VITE_API_URL=http://localhost:8000/api/v1
